services:
  app:
    build: .
    container_name: pavel-maria-wedding
    restart: always
    env_file: .env
    environment:
      - DB_PATH=/app/data/submissions.db
    volumes:
      - ./data:/app/data
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
